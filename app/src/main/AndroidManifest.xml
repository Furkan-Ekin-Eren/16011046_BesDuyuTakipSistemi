<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.asdf">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".testMest"></activity>
        <activity android:name=".mediaAyrinti" />
        <activity android:name=".mediaShowPanel" />
        <activity android:name=".mediaAddPanel" />
        <activity android:name=".customerAddPanel" />
        <activity android:name=".customerPanel" />
        <activity android:name=".mediaPanel" />
        <activity android:name=".companyPanel" />
        <activity android:name=".girisEkran">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Ayrinti" />
        <activity
            android:name=".ekran3"
            android:label="@string/title_activity_ekran3" />
        <activity
            android:name=".ekran2"
            android:label="@string/title_activity_ekran2"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>